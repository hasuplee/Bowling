# PRD.md — Bowling Kata 점수 계산기

## 배경 / 목적

볼링 경기의 투구 기록을 입력받아 최종 점수를 계산하는 로직을 TDD로 구현한다. 목적은 실제 서비스가 아니라 TDD 사이클(RED → GREEN → REFACTOR)을 연습하는 것이다. UI, 영속성(저장), 멀티플레이어, 네트워크 등은 다루지 않는다.

## 범위

### In Scope
- 한 게임(1인, 10프레임)의 투구를 순서대로 기록하고, 최종 점수를 계산하는 라이브러리 레벨 API
- 오픈 프레임 / 스페어 / 스트라이크 / 퍼펙트 게임 / 10번째 프레임 특수 규칙의 정확한 점수 계산

### Out of Scope
- CLI/UI, 파일/DB 저장, 여러 선수 동시 진행, 점수판 시각화
- 잘못된 입력(예: 음수 핀, 한 프레임에서 10개 초과, 유효 범위를 벗어난 `roll()` 호출 등)에 대한 방어적 검증 — 별도 요청이 있을 때까지는 다루지 않는다.

## 요구사항 (기능)

### API

`Game` 클래스 하나로 구현한다.

- `Game()`: 새 게임 인스턴스를 생성한다. 초기 상태에서 점수는 0이다.
- `Game.roll(pins: int) -> None`: 한 번의 투구에서 쓰러뜨린 핀의 개수를 기록한다.
- `Game.score() -> int`: 지금까지 기록된 투구를 바탕으로 10프레임 전체의 최종 점수를 계산해 반환한다.

### 점수 규칙

1. **오픈 프레임**: 한 프레임에서 두 번 던져 10개 미만을 쓰러뜨리면, 그 두 투구의 합이 해당 프레임 점수다.
2. **스페어**: 한 프레임에서 두 번 던져 정확히 10개를 쓰러뜨리면, 10 + 다음 1번 투구의 핀 수가 해당 프레임 점수다.
3. **스트라이크**: 첫 번째 투구에서 10개를 모두 쓰러뜨리면, 10 + 다음 2번 투구의 핀 수 합이 해당 프레임 점수다.
4. **10번째 프레임**: 스페어나 스트라이크가 나오면 보너스 계산용 투구 기회가 추가된다(스페어는 1회 추가로 총 3구, 스트라이크는 2회 추가로 총 3구). 이 보너스 투구 자체는 다시 스트라이크/스페어가 되어도 추가 기회를 주지 않는다.
5. **최종 점수**는 1~10프레임 점수의 합이다.

## 수용 기준 (Acceptance Criteria) — 검증할 시나리오 목록

아래 시나리오들이 모두 정확한 점수를 반환해야 완료로 본다. (구현 순서는 `Plan.md` 참고)

1. 거터 게임: 20번의 투구가 모두 0핀 → `score() == 0`
2. 전부 1핀: 20번의 투구가 모두 1핀 → `score() == 20`
3. 스페어 1회 포함, 나머지는 거터: `score()`가 스페어 보너스(다음 1구)를 정확히 반영
4. 스트라이크 1회 포함, 나머지는 거터: `score()`가 스트라이크 보너스(다음 2구)를 정확히 반영
5. 퍼펙트 게임: 12연속 스트라이크(10번째 프레임 보너스 2구 포함) → `score() == 300`
6. 10번째 프레임이 스페어인 경우: 보너스 투구 1회가 정확히 반영되고, 그 이상의 추가 기회는 주어지지 않는다
7. 10번째 프레임이 스트라이크인 경우: 보너스 투구 2회가 정확히 반영되고, 보너스 투구가 다시 스트라이크여도 추가 기회는 없다
8. 여러 프레임에 걸쳐 오픈/스페어/스트라이크가 섞인 일반적인 게임에서도 정확한 합산 점수를 반환한다

## 비기능 요구사항

- 외부 의존성 없이 표준 라이브러리 + pytest만 사용한다.
- 모든 프로덕션 코드는 이를 검증하는 실패 테스트가 먼저 존재한 뒤에만 작성한다 (`.claude/TDD/SKILL.md` 준수).
- 테스트 코드는 반드시 `tests/` 폴더 안에 작성한다 (예: `tests/test_game.py`).
