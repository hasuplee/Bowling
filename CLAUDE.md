# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(및 사람 파트너)를 위한 가이드다.

## 프로젝트 개요

Robert C. Martin이 만든 **Bowling Kata**를 TDD로 구현하는 연습 프로젝트다. 실제 볼링 게임 UI가 아니라 **볼링 점수 계산 로직**만을 다룬다. 요구사항 상세는 `PRD.md`, 진행 계획은 `Plan.md`, 볼링 규칙 개요는 `README.md`를 참고한다.

- 언어: Python
- 테스트 프레임워크: pytest
- 소스 구조: 프로젝트 루트의 `game.py` (필요시 이후 `src/bowling/`로 리팩터링), 테스트는 `tests/test_game.py`

## 개발 환경

```bash
# 가상환경(.venv)이 이미 생성되어 있다고 가정
pytest                          # 전체 테스트 실행
pytest tests/test_game.py -v    # 단일 파일 실행, verbose
pytest -x                       # 첫 실패에서 즉시 중단 (RED 확인용)
```

## TDD 워크플로우 (필수)

이 저장소의 모든 구현/버그 수정은 `.claude/TDD/SKILL.md`의 **test-driven-development** 스킬을 그대로 따른다. 핵심 요약:

1. **RED**: `Plan.md`에 이번 사이클의 목표·범위·테스트 계획을 작성 → 사람 파트너 승인 → 승인된 계획대로 실패하는 테스트 작성 → 실패를 직접 확인한다.
2. **GREEN**: Plan.md의 goal만 만족시키는 최소한의 구현을 작성하고 테스트를 통과시킨다. 스코프 밖의 기능을 미리 만들지 않는다.
3. **REFACTOR/REVIEW**: 그린 상태를 유지하며 필요한 리팩토링이 있는지 검토한다. 리팩토링이 필요하면 어떤 리팩토링인지 사람 파트너에게 제안하고 승인받은 뒤에만 수행한다.

테스트 함수는 반드시 `def test_한글...` 형식으로 작성한다 (예: `def test_거터_게임의_점수는_0이다():`).

**철의 법칙**: 실패하는 테스트가 먼저 존재하지 않는 한 프로덕션 코드를 작성하지 않는다.

## 이 프로젝트의 커밋 규칙 (SKILL.md 기본값에 대한 커스터마이즈)

`.claude/TDD/SKILL.md`의 기본 워크플로우는 RED 종료 시점과 REVIEW 종료 시점, 두 곳에서만 커밋한다. **이 프로젝트에서는 이를 확장하여 매 사이클마다 RED / GREEN / REFACTOR 세 단계 각각에서 커밋한다:**

1. **RED 커밋**: 실패하는 테스트를 작성하고 실패를 확인한 직후. `Plan.md` + 새로 작성한 실패 테스트를 커밋 대상으로 한다.
2. **GREEN 커밋**: 테스트를 통과시키는 최소 구현을 완료하고 전체 테스트가 그린임을 확인한 직후. 구현 코드를 커밋 대상으로 한다.
3. **REFACTOR 커밋**: 리팩토링을 수행하고 테스트가 여전히 그린임을 재확인한 직후. 리팩토링한 코드를 커밋 대상으로 한다. (해당 사이클에 리팩토링이 없다면 이 커밋은 생략한다.)

**세 커밋 시점 모두, 커밋을 실행하기 전에 반드시 사람 파트너에게 커밋 여부를 확인받는다.** 확인 없이 임의로 `git commit`을 실행하지 않는다.

커밋 메시지에는 어느 사이클, 어느 단계(RED/GREEN/REFACTOR)인지, 그리고 무엇을 검증/구현했는지 명확히 드러낸다. 예:

```
[Cycle 1][RED] 거터 게임 점수 0점 실패 테스트 추가
[Cycle 1][GREEN] Game.roll/score 최소 구현으로 거터 게임 테스트 통과
[Cycle 3][REFACTOR] is_spare/is_strike 헬퍼 함수로 점수 계산 로직 정리
```

## 사이클 진행 순서

`Plan.md`에 명시된 순서(거터 게임 → 전부 1핀 → 스페어 → 스트라이크 → 퍼펙트 게임 → 10번째 프레임 특수 규칙)를 따른다. 각 사이클을 시작하기 전, 사람 파트너에게 다음 사이클의 `Plan.md` 갱신 내용을 제시하고 승인을 받는다.
