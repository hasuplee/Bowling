# Plan.md

TDD 사이클 진행 계획. 각 사이클은 RED(테스트 작성) → GREEN(최소 구현) → REFACTOR(필요시 정리) 순서로 진행하며, 세 단계 각각 사람 파트너 승인 후 커밋한다 (`CLAUDE.md`의 커밋 규칙 참고).

## 전체 사이클 로드맵

| # | 사이클 이름 | 목표 |
|---|---|---|
| 1 | 거터 게임 (Gutter Game) | 20번의 투구가 모두 0핀일 때 `score() == 0` |
| 2 | 올원 게임 (All Ones) | 20번의 투구가 모두 1핀일 때 `score() == 20` |
| 3 | 스페어 1회 (One Spare) | 스페어의 보너스(다음 1구)가 정확히 합산됨 |
| 4 | 스트라이크 1회 (One Strike) | 스트라이크의 보너스(다음 2구)가 정확히 합산됨 |
| 5 | 퍼펙트 게임 (Perfect Game) | 12연속 스트라이크 → `score() == 300` |
| 6 | 10번째 프레임 스페어 | 10프레임이 스페어일 때 보너스 투구 1회만 추가로 주어짐 |
| 7 | 10번째 프레임 스트라이크 | 10프레임이 스트라이크일 때 보너스 투구 2회만 추가로 주어짐 |
| 8 | 혼합 게임 | 오픈/스페어/스트라이크가 섞인 일반적인 게임에서도 정확히 합산 |

(2~8번은 이전 사이클 GREEN 이후, 각 사이클 시작 시점에 이 표를 참고해 아래 "이번 사이클 상세"를 갱신하고 사람 파트너 승인을 받는다.)

---

## 이번 사이클 상세: Cycle 1 — 거터 게임 (Gutter Game)

### 목표 동작 (Behavior)

20번의 투구가 모두 0핀일 때, `Game.score()`가 0을 반환한다.

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_거터_게임의_점수는_0이다`
- 입력/셋업: 새로운 `Game()` 인스턴스 생성 후 `roll(0)`을 20회 호출
- 기대 결과 (assertion): `game.score() == 0`
- 왜 이 케이스가 가치 있는가?
  - 가장 단순한 시나리오로 `Game`의 기본 골격(`__init__`, `roll`, `score`)을 강제한다.
  - 스페어/스트라이크가 없는 "정상 프레임"의 합산 로직을 가장 단순한 형태로 검증한다.
  - 이후 사이클(올원 → 스페어 → 스트라이크 → 퍼펙트 → 10프레임)의 토대가 된다.

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 0이 아닌 핀 수의 처리 (Cycle 2에서 다룸)
- 스페어/스트라이크 보너스 로직
- 10프레임 특수 규칙
- 입력 검증 (PRD.md에 out of scope로 명시)

### 예상 실패 메시지

`game.py`와 `Game` 클래스가 아직 존재하지 않으므로:

```
ModuleNotFoundError: No module named 'game'
```

(또는 `tests/test_game.py` 파일이 새로 작성되어야 하므로 임포트 단계에서 실패)

---

## 이번 사이클 상세: Cycle 4 — 스트라이크 1회 (One Strike)

### 목표 동작 (Behavior)

첫 프레임이 스트라이크(첫 투구에 10핀)이면, 다음 2구의 합이 첫 프레임 점수에 보너스로 더해져야 한다.

- 게임 시퀀스: `10, 3, 4, 0, 0, ... (총 19구: 스트라이크 1구 + 나머지 18구)`
- 프레임별 점수: 프레임1 = 10+3+4(보너스) = 17, 프레임2 = 3+4 = 7, 나머지(프레임3~10) = 0
- 합계: 17 + 7 = 24

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_한_번의_스트라이크는_다음_2구를_보너스로_받는다`
- 입력/셋업:
  - `Game()` 생성
  - `roll(10)` (스트라이크, 1구만 소비)
  - `roll(3)`, `roll(4)` (프레임2의 두 투구이자 프레임1의 보너스)
  - `roll(0)` 16회 (나머지 투구, 총 19구)
- 기대 결과 (assertion): `game.score() == 24`
- 왜 이 케이스가 가치 있는가?
  - 스트라이크는 한 프레임에서 **1구만 소비**한다는 점이 스페어와 다르며, `roll_index` 증가 폭을 프레임마다 가변적으로 만들어야 함을 강제한다.
  - 보너스가 다음 "2구"(다음 프레임의 롤들, 프레임 경계를 넘나들 수 있음)임을 검증한다.
  - Cycle 5(퍼펙트 게임)에서 연속 스트라이크가 겹칠 때의 토대가 된다.

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 연속된 스트라이크(다음 프레임도 스트라이크인 경우) — Cycle 5(퍼펙트 게임)에서 다룸
- 10프레임의 스트라이크 특수 처리 — Cycle 7에서 다룸

### 예상 실패 메시지

현재 구현은 프레임마다 `roll_index`를 항상 2씩 증가시키므로, 스트라이크(1구 소비)를 만나면 롤 인덱스가 어긋나 잘못된 값을 계산하거나 `IndexError`가 발생할 것으로 예상된다. RED 검증 시 실제 오류 종류를 확인한다.

**결과**: 예상대로 `IndexError` 발생 (커밋: `[Cycle 4][RED]`). GREEN 단계에서 `_is_strike` 헬퍼와 `roll_index += 1` 분기를 추가해 통과시킴 (커밋: `[Cycle 4][GREEN]`). 리팩토링 불필요로 REFACTOR 단계 생략.

---

## 이번 사이클 상세: Cycle 5 — 퍼펙트 게임 (Perfect Game)

### 목표 동작 (Behavior)

12연속 스트라이크(10프레임까지 각 프레임 스트라이크 + 마지막 프레임 보너스 2구도 스트라이크)일 때 `Game.score()`가 300을 반환한다.

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_퍼펙트_게임의_점수는_300이다`
- 입력/셋업: `Game()` 생성 후 `roll(10)`을 12회 호출
- 기대 결과 (assertion): `game.score() == 300`
- 왜 이 케이스가 가치 있는가?
  - 연속된 스트라이크가 겹칠 때(각 프레임의 보너스가 다음 프레임들의 스트라이크 자체와 겹침) 인덱스 계산이 정확한지 검증한다.
  - 10프레임에서 정확히 12구만 소비하고 멈추는지(더 많은 롤이 있어도 10프레임 루프만 도는지) 확인한다.

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 10프레임이 스페어인 경우의 보너스 처리(Cycle 6)
- 10프레임이 스트라이크지만 이후 다른 조합인 일반적인 경우(Cycle 7)

### 예상 결과

Cycle 4에서 구현한 `_is_strike` 분기(`roll_index += 1`)가 이미 연속 스트라이크를 프레임 경계 없이 순회하므로, **이 테스트는 추가 구현 없이 즉시 통과할 가능성이 높다**. RED 검증 시 실제 결과를 확인하고, 즉시 통과하면 회귀 방지용 테스트로 유지한 채 다음 사이클로 넘어간다.

**결과**: 예상대로 RED 없이 즉시 PASSED. 사람 파트너 확인 결과 회귀 방지용 테스트로 유지하고, 별도 GREEN/REFACTOR 구현 없이 Cycle 6(10프레임 스페어)으로 진행.

---

## 이번 사이클 상세: Cycle 6 — 10번째 프레임 스페어

### 목표 동작 (Behavior)

10번째(마지막) 프레임이 스페어(두 투구 합 10)이면, 보너스로 다음 1구(총 3구째 투구)가 추가되어 합산되고, 그 이상의 추가 기회는 주어지지 않는다.

- 게임 시퀀스: 1~9프레임은 모두 거터(0,0)×9=18구, 10프레임은 `5, 5, 3` (스페어 + 보너스 1구, 총 21구)
- 프레임별 점수: 프레임1~9 = 0, 프레임10 = 5+5+3(보너스) = 13
- 합계: 13

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_10프레임이_스페어면_보너스_1구만_추가된다`
- 입력/셋업:
  - `Game()` 생성
  - `roll(0)` 18회 (1~9프레임)
  - `roll(5)`, `roll(5)`, `roll(3)` (10프레임: 스페어 + 보너스 1구)
- 기대 결과 (assertion): `game.score() == 13`
- 왜 이 케이스가 가치 있는가?
  - 10프레임에서 정확히 3구까지만 입력되고 더 이상의 투구가 없어도 `score()`가 정상 동작하는지 검증한다.
  - Cycle 3의 스페어 로직이 마지막 프레임에서도 일반화되어 동작하는지 확인한다.

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 10프레임이 스트라이크인 경우 (Cycle 7에서 다룸)

### 예상 결과

Cycle 3에서 구현한 스페어 분기는 프레임 번호를 구분하지 않고 롤 배열 위치만으로 판단하므로, **이 테스트도 추가 구현 없이 즉시 통과할 가능성이 높다**. RED 검증 시 실제 결과를 확인한다.

**결과**: 예상대로 RED 없이 즉시 PASSED. 현재 알고리즘이 프레임 번호를 구분하지 않고 정확히 10회만 순회하는 방식이라 10프레임도 이미 일반화되어 처리됨. 사람 파트너 확인 결과 회귀 방지용 테스트로 유지하고 Cycle 7(10프레임 스트라이크)로 진행.

---

## 이번 사이클 상세: Cycle 7 — 10번째 프레임 스트라이크

### 목표 동작 (Behavior)

10번째(마지막) 프레임이 스트라이크(첫 투구 10핀)이면, 보너스로 다음 2구가 추가되어 합산되고, 그 보너스 투구가 다시 스트라이크/스페어여도 그 이상의 추가 기회는 주어지지 않는다.

- 게임 시퀀스: 1~9프레임은 모두 거터(0,0)×9=18구, 10프레임은 `10, 5, 3` (스트라이크 + 보너스 2구, 총 21구)
- 프레임별 점수: 프레임1~9 = 0, 프레임10 = 10+5+3(보너스) = 18
- 합계: 18

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_10프레임이_스트라이크면_보너스_2구만_추가된다`
- 입력/셋업:
  - `Game()` 생성
  - `roll(0)` 18회 (1~9프레임)
  - `roll(10)`, `roll(5)`, `roll(3)` (10프레임: 스트라이크 + 보너스 2구, 총 21구)
- 기대 결과 (assertion): `game.score() == 18`
- 왜 이 케이스가 가치 있는가?
  - 10프레임 스트라이크의 보너스가 정확히 2구까지만 반영되고 그 이상을 요구하지 않는지 확인한다(투구 배열이 정확히 21개로 끝나도 `IndexError` 없이 동작해야 함).

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 없음 (이 사이클로 PRD.md의 모든 수용 기준 1~7이 충족됨)

### 예상 결과

Cycle 4에서 구현한 스트라이크 분기는 프레임 번호를 구분하지 않고 롤 배열 위치만으로 판단하며, 루프 자체가 정확히 10회로 고정되어 있어 10프레임 이후 추가 순회가 없다. **이 테스트도 추가 구현 없이 즉시 통과할 가능성이 높다**. RED 검증 시 실제 결과를 확인한다.

**결과**: 예상대로 RED 없이 즉시 PASSED. 사람 파트너 확인 결과 회귀 방지용 테스트로 유지하고 Cycle 8(혼합 게임)로 진행. PRD.md 수용 기준 1~7 모두 충족됨.

### GREEN 단계 예상 구현 스케치 (참고용, 실제는 RED 후 작성)

```python
class Game:
    def __init__(self):
        self._rolls = []

    def roll(self, pins):
        self._rolls.append(pins)

    def score(self):
        return sum(self._rolls)
```

**결과**: 위 구현대로 GREEN 완료 (커밋: `[Cycle 1][GREEN]`). 리팩토링 불필요로 REFACTOR 단계 생략.

---

## 이번 사이클 상세: Cycle 2 — 올원 게임 (All Ones)

### 목표 동작 (Behavior)

20번의 투구가 모두 1핀일 때, `Game.score()`가 20을 반환한다.

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_모든_투구가_1핀이면_점수는_20이다`
- 입력/셋업: 새로운 `Game()` 인스턴스 생성 후 `roll(1)`을 20회 호출
- 기대 결과 (assertion): `game.score() == 20`
- 왜 이 케이스가 가치 있는가?
  - 0이 아닌 핀 수를 누적 합산하는 동작을 강제한다.
  - 스페어/스트라이크가 발생하지 않는 "정상 프레임"의 합산을 거터 게임 외에 한 번 더 검증한다.

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 스페어/스트라이크 보너스 로직 (Cycle 3, 4에서 다룸)
- 10프레임 특수 규칙
- 입력 검증

### 예상 실패 메시지

**주의**: 현재 `Game.score()`는 `sum(self._rolls)`로 이미 구현되어 있어, 이 테스트는 추가 구현 없이도 즉시 통과(PASS)할 가능성이 높다. 즉, 엄격한 의미의 RED(실패)를 만들지 못할 수 있다.

- 만약 즉시 통과한다면: 이 테스트는 회귀 방지용 테스트로만 유지하고, GREEN 단계에서 추가 구현 없이 다음 사이클(Cycle 3, 스페어)로 넘어간다.
- RED 검증 시 실제 결과를 사람 파트너에게 보고하고 위 처리 방식에 대해 확인받는다.

**결과**: 예상대로 RED 없이 즉시 PASSED (기존 `sum()` 구현이 우연히 충족). 사람 파트너 확인 결과 회귀 방지용 테스트로 유지하고, 별도 GREEN/REFACTOR 구현 없이 Cycle 3(스페어)로 진행하기로 함.

---

## 이번 사이클 상세: Cycle 3 — 스페어 1회 (One Spare)

### 목표 동작 (Behavior)

첫 프레임이 스페어(5+5=10)이고 그 다음 투구가 3이면, 첫 프레임 점수에 다음 1구(3)가 보너스로 더해져야 한다.

- 게임 시퀀스: `5, 5, 3, 0, 0, ... (총 20구)`
- 프레임별 점수: 프레임1 = 5+5+3(보너스) = 13, 프레임2 = 3+0 = 3, 나머지 = 0
- 합계: 13 + 3 = 16

### 테스트 계획

- 테스트 파일: `tests/test_game.py`
- 테스트 이름: `test_한_번의_스페어는_다음_1구를_보너스로_받는다`
- 입력/셋업:
  - `Game()` 생성
  - `roll(5)`, `roll(5)` (스페어)
  - `roll(3)` (보너스 + 프레임2의 첫 투구)
  - `roll(0)` 17회 (나머지 투구, 총 20구)
- 기대 결과 (assertion): `game.score() == 16`
- 왜 이 케이스가 가치 있는가?
  - 단순 `sum()`으로는 절대 풀 수 없음 → **진정한 RED**가 보장됨 (`sum()`은 5+5+3+0*17=13을 반환하므로 기대값 16과 다름).
  - 스페어(두 롤의 합이 10)라는 정의와 "다음 1롤을 보너스로 더한다"는 규칙을 동시에 강제한다.
  - 다음 사이클(스트라이크)의 보너스 로직 일반화의 토대가 됨.

### 다루지 않는 것 (Out of Scope, 이번 사이클)

- 스트라이크 (Cycle 4에서 다룸)
- 두 개 이상의 스페어 (Out of Scope — 한 케이스에 집중)
- 10프레임의 스페어/스트라이크 특수 처리
- 마지막 프레임이 스페어인 경우의 보너스

### 예상 실패 메시지

현재 `score()`는 `sum(self._rolls)`를 그대로 반환하므로:

```
assert 13 == 16
```

(20개 롤의 단순 합 13 vs 기대값 16 — 명확한 RED)
